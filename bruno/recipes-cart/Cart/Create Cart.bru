meta {
  name: Create Cart
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/carts
  body: none
  auth: inherit
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("_cart-id", data.id);
}

settings {
  encodeUrl: true
}
